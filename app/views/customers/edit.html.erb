<h1>Editing <%= "#{@customer.name}" %></h1>

<%= render 'form' %>

<hr>


<h4> Add Deployment for <%= "#{@customer.name}" %></h4>
<%= render 'deployments/form' %>




<%= link_to 'Back', customers_path, :class => "btn btn-link" %>
