%h1 Listing deployments

%table
  %tr
    %th Customer
    %th Deployment Date
    %th Version
    %th
    %th
    %th



  - @deployments.each do |deployment|
    %tr
      %td= deployment.customer.name
      %td= deployment.deploy_date
      %td= deployment.release.version
      %td= link_to 'Show', deployment
      %td= link_to 'Edit', edit_deployment_path(deployment)
      %td= link_to 'Destroy', deployment, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Deployment', new_deployment_path
= link_to 'New Customer', new_customer_path
= link_to 'New Release', new_release_path
